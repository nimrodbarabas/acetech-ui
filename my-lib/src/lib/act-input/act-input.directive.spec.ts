import { ActInputDirective } from './act-input.directive';

describe('ActInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ActInputDirective();
    expect(directive).toBeTruthy();
  });
});
